# Gemini fixtures (M4)

Stub / redacted shapes for unit tests. No live cookies or tokens.

| File                          | Purpose                                    |
| ----------------------------- | ------------------------------------------ |
| `history.hits.stub.json`      | History rail items including a title match |
| `history.empty.stub.json`     | Authenticated empty history window         |
| `history.with-body.stub.json` | Ensures normalizer drops body fields       |
